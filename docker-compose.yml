
---

## **3. docker-compose.yml**
```yaml
version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./scripts:/scripts
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
